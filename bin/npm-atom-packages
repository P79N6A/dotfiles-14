#!/bin/bash

# paths=("ftp-remote-edit menu-manager jquery-snippets chrome-color-picker javascript-snippets sort-lines autocomplete-json split-diff aligner-php blade-snippets js-hyperclick command-toolbar tool-bar project-manager php-hyperclick double-tag hyperclick css-hyperclick v-bootstrap4 aligner-css autocomplete-font-awesome script emmet aligner-scss pretty-json artisan intentions aligner-coffeescript change-case sass-autocompile browser-plus aligner-javascript busy-signal autocomplete-sql advanced-open-file language-blade autocomplete-js-import atom-autocomplete-php remote-sync remote-ftp aligner")

# for item in $paths; do
#   (cd ./packages/$item && npm install)
#   apm install $item
# done

packages=("atom-package-deps")

for item in $packages; do
  (cd ~/.atom && npm install $item)
done
