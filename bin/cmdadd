#!/bin/bash

if [[ ! -n $* ]]; then
__return "
  command: `basename $0`
  usage  : file [target_dir] [options]

  options
    -o Open file for edit after created.
"; exit 1;
fi

argv=($*);
isOpenFile="";

echo "${argv[@]}";
